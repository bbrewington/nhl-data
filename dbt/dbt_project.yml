name: 'moneypuck_dbt'
version: '1.0.0'

profile: 'moneypuck_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  moneypuck_dbt:
    staging:
      +materialized: table
    intermediate:
      +materialized: view

