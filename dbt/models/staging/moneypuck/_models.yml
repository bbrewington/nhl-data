version: 2

models:
  - name: stg__mp__team_game_situation
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - team_abbr
            - game_id
            - situation
